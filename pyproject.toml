[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nanogpt"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
nanogpt = "nanogpt.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]
